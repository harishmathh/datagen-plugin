---
description: Research the real-world domain behind a business and produce a visual HTML report. Research only, no dataset generated.
argument-hint: <business context> | objective: <...> | comments: <...>
---

Run the **Dataset-Research** skill.

This is research only. It grounds a business context and an objective in
real-world data and produces a beautiful, illustrated HTML report. It does not
build a recipe and it does not generate any data.

The user's request follows. Pull the business context and the objective out of
it (comments are optional). If either the business context or the objective is
missing, ask for it and wait before doing anything else.

User request:
$ARGUMENTS
